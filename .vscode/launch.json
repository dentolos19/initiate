{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "App",
      "type": "node-terminal",
      "request": "launch",
      "command": "just start",
      "preLaunchTask": "Setup",
      "serverReadyAction": {
        "pattern": "Local:.+(https?://.+)",
        "uriFormat": "%s",
        "action": "openExternally"
      }
    }
  ]
}
