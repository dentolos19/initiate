{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "App",
      "type": "node-terminal",
      "request": "launch",
      "command": "make start",
      "postDebugTask": "Stop",
      "serverReadyAction": {
        "pattern": "Local:.+(https?://.+)",
        "uriFormat": "%s",
        "action": "openExternally"
      }
    }
  ]
}
